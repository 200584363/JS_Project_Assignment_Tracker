<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Assignment Tracker</a>

    <div class="d-flex">
      {{#if user}}
        <span class="navbar-text me-2">Hello, {{user.username}}</span>
        <a class="btn btn-outline-primary btn-sm me-2" href="/assignments">Dashboard</a>
        <a class="btn btn-outline-danger btn-sm" href="/auth/logout">Logout</a>
      {{else}}
        <a class="btn btn-outline-primary btn-sm me-2" href="/auth/login">Login</a>
        <a class="btn btn-outline-success btn-sm" href="/auth/register">Register</a>
      {{/if}}
    </div>
  </div>
</nav>
